# Copy to .env locally. Never commit real values.

# Application
APP_ENV=development
APP_BASE_URL=http://localhost:3000

# Database (fill after the stack decision)
DATABASE_URL=

# Object storage (fill after the storage decision)
STORAGE_ENDPOINT=
STORAGE_BUCKET=
STORAGE_ACCESS_KEY=
STORAGE_SECRET_KEY=

# Authentication (fill after the authentication decision)
AUTH_SECRET=

